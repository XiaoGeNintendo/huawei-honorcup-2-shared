import picHelper as pic
import keras
import random

size=64
width=32

def getTrainData(cnt):
    x_train=numpy.empty((cnt,size,width))
    y_train=numpy.empty((cnt,1))
    for i in range(cnt):
        if random.randint(0,1)==0:
            y_train[i]=0
            x_train[i]=
    
    
    
